// Para executar o programa, necessáriamente devemos compila-lo em um executável. 

#include <stdio.h>  // Standard (std) input (i) output (o) . h (header file) utilizada para leitura / processamento e reconhecimento de dados externos 
                    // Como por exemplo as funções printf() e fscan()

int main ( ){                    // Dentro de parêntesis mantemos os ( parâmetros ) da função, e dentro de chaves { tudo o que será executado pela máquina - função } 
    printf("Hello, World \n");   // Função pré-programada em stdio.h para printar informações em CLI + \n (subcomando para pular uma linha).
    
    
}


